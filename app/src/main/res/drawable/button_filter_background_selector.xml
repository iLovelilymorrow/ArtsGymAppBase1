<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when button is checked -->
    <item android:state_checked="true"
        android:drawable="@drawable/border_background_grey_corner_checked" />

    <!-- Default state (not checked) -->
    <item android:drawable="@drawable/border_background_grey_corner"/>
</selector>